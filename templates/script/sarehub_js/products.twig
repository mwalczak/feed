<script type="text/javascript">
    //SAREhub Integration
    document.addEventListener('click', function(e){
        if(e.srcElement.classList.contains('add-to-cart')){
            const productId = e.srcElement.dataset.productId.toString();
            const execute_params = {'_userId': '', '_email' : '{{ email }}', '_cartadd' : {'country' : 'PL', 'language': 'pl', 'cart_id' : '{{ sessionId }}', 'product_id' : productId, 'quantity' : 1}};
            console.log(execute_params);
            sareX_core.execute(10, execute_params);
        }
    });
</script>